{% extends '@gech_app/layout/page.html.twig' %}

{% block inside_main %}

    <h1 class="mt-3">{{ "Soumettre une anecdote"|t }}</h1>

    <div class="row">
        <div class="col-md-8">
            {% set anecdote_content = bamboo_config_get('ngeo_form.settings', 'anecdote_content') %}
            {{ anecdote_content.value|raw }}
            {{ bamboo_render_form('ngeo_form', 'AnecdoteForm') }}
        </div>
    </div>

{% endblock %}